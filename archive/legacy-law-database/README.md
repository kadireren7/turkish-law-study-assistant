# Legacy `law-database/` snapshot

This folder preserves the pre-migration duplicate of selected statute excerpts that lived at repo root as `law-database/`. The application now reads canonical legislation only from `data/core/laws/`. Keep this archive for diff or audit; do not wire it back into runtime code.
