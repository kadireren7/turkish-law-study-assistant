# Architecture

StudyLaw uses a **source-grounded** Next.js app: the OpenAI API generates text only over retrieved Turkish legal content from the local `data/` tree. See the root `README.md` for the full product and policy overview.
