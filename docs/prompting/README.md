# Prompting

System and feature prompts live under `src/lib/*-prompt.ts` and `src/lib/legal-education-master-prompt.ts`. They assume **official-source priority** and local `data/` context injected at runtime.
